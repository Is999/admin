groups:
  - name: admin-cron
    rules:
      - alert: AdminCronCollectorProcessorFailed
        expr: sum by (biz_type) (rate(admin_cron_collector_processor_events_total{result!="success"}[5m])) > 0
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: "admin-cron Collector Processor failed"
          description: "Collector biz_type={{ $labels.biz_type }} has failed processor events in the last 10 minutes."

      - alert: AdminCronCollectorProcessorHighLatency
        expr: histogram_quantile(0.95, sum by (le, biz_type) (rate(admin_cron_collector_processor_batch_duration_seconds_bucket[5m]))) > 5
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: "admin-cron Collector Processor latency is high"
          description: "Collector biz_type={{ $labels.biz_type }} P95 batch duration is above 5 seconds."

      - alert: AdminCronCollectorOutboxFallbackSpike
        expr: sum by (transport) (rate(admin_cron_collector_outbox_persist_events_total[5m])) > 10
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: "admin-cron Collector outbox fallback spike"
          description: "Collector transport={{ $labels.transport }} is writing more than 10 outbox events per second."
